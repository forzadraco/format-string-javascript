{
  "name": "fmt-string",
  "version": "1.1.1",
  "description": "lightweight string templating with object, object of object, array of object and also python 3 str.format equivalent",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "string format",
    "string template",
    "format string with array",
    "format string with object",
    "minimal",
    "lightweight"
  ],
  "author": "Ardy Satria Hasanuddin",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "mocha": "^6.0.2"
  }
}
